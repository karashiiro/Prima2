name: Prima Google APIs

on:
  push:
    branches: [ main ]
    paths:
    - "src/prima-google-apis/**"
  pull_request:
    branches: [ main ]
    paths:
    - "src/prima-google-apis/**"

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Build
      run: |
        cd src/prima-google-apis
        go build -v ./...

    - name: Test
      run: |
        cd src/prima-google-apis
        go test -v ./...
